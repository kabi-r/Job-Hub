import React from 'react';

const Blog = () => {
    return (
        <div className='my-container'>
            <h3 className='text-xl text-red-500 mt-5 my-3'>How should i context api?</h3>
            <p>React context api  need to use when you want to data  accessible by many components at different nesting levels in your project.IT helps to pass the data and anywhere to using make a components.IT first called parent section then any time any where i will using the data.Best top likely context api in nested component build to easy in your project</p>

            <h3 className='text-xl text-red-500 mt-5 my-3'>What is Custom Hook?</h3>
            <p>Custom hook is a reusale function to use anywhere need a uniqe function you can use react special hook in help your project. React huge hook have it to make component and call or data store .Your cooding logical function use to need hooks.</p>

            <h3 className='text-xl text-red-500 mt-5 my-3'>useRef Description</h3>
            <p>Custom hook is a reusale function to use anywhere need a uniqe function you can use react special hook in help your project. React huge hook have it to make component and call or data store .Your cooding logical function use to need hooks.</p>

            <h3 className='text-xl text-red-500 mt-5 my-3'>useMemo description </h3>
            <p>React.memo is a higher order component that's used to wrap a React functional component. The way it works is:- React does an initial render of the component when it first loads and stores it in memory.</p>
            
            
        </div>
    );
};

export default Blog;